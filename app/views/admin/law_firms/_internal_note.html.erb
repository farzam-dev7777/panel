<li>
  <div class="rotate-1 yellow-bg">
    <small>
    	<strong><%= note.created_at.strftime(LawFirm::TIME_FORMAT) %></strong>
  	</small>
    <p><%= note.message %></p>
    <!-- <a data-internal_note_id=<%= note.id %> data-id="<%= @law_firm.id %>" href="#" class="text-danger pull-right delete-btn remove-internal-note"><i class="fa fa-trash-o "></i></a> -->
  </div>
</li>