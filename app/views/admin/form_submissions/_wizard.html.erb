<div class="form-bootstrapWizard">
  <ul class="bootstrapWizard form-wizard">
    <%- steps.each_with_index do |step, index| %>
      <li class="<%= (steps.find_index(step) <= steps.find_index(current_step)) ? 'active' : ''%>">
        <%= link_to wizard_path(step) do %>
          <span class="step"><%= index + 1 %></span> <span class="title"><%= step.capitalize %></span>
        <% end %>
      </li>
    <% end %>
  </ul>
  <div class="clearfix"></div>
</div>
<br />
<br />
<br />