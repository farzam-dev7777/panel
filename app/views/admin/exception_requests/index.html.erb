<% content_for :page_title do %>
<span>Exception Requests</span>
<% end %>
<div class="clearfix"></div>
<div class="col-lg-12">
  <div class="jarviswidget jarviswidget-color-darken" id="wid-id-0" data-widget-editbutton="false">
    <header>
      <span class="widget-icon"> <i class="fa fa-briefcase"></i> </span>
      <h2>Exception Requests</h2>
    </header>

    <%= search_form_for [:admin, @q] do |f| %>

      <%= f.label :requested_by_cont %>
      <%= f.search_field :requested_by_cont %>

      <%= f.label :submitted_by_email_cont %>
      <%= f.search_field :submitted_by_email_cont %>

      <%= f.label :law_firm_id_eq %>
      <%= f.select :law_firm_id_eq, options_from_collection_for_select(LawFirm.all, "id", "name", @q.law_firm_id_eq), {:include_blank => 'Select law firm'} %>

      <%= f.submit "Search", class: "btn btn-primary" %>
    <% end %>

    <% if current_user.role == "lob" %>
		  <div class="no-padding">
				<div class="widget-body">
					<div id="myTabContent" class="tab-content">
						<div class="tab-pane fade active in padding-10 no-padding-bottom" id="s1">
							<div class="row no-space text-center">
								<div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
									<p><b><%= certification_status(current_law_firm) %></b> 
									<b><%= link_to 'Submit New Exception Request', new_admin_exception_request_path, html_options = {class: 'btn btn-primary btn-lg dashboard-certificate-button'}   %></b></p>
								</div>
							</div>
						</div>
					</div>
				</div>
      </div>
    <% end %>  
    <!-- widget div-->
    <div>
      <!-- widget edit box -->
      <div class="jarviswidget-editbox">
        <!-- This area used as dropdown edit box -->
      </div>
      <!-- end widget edit box -->
      <!-- widget content -->
      <div class="widget-body no-padding">
        <table id="dt_basic" class="table table-striped table-bordered table-hover" width="100%">
          <thead>
            <tr style="background: #007ac3;color: #fff;">
              <th>Requested By</th>
              <th>Submitted By</th>
              <th>LOB Contact Name</th>
              <th>Action</th>
            </tr>
          </thead>
          <tbody>
            <% @exception_requests.each do |exception_request| %>
            <tr class="" data-id="<%= exception_request.id %>">
              <td><%= exception_request.requested_by %></td>
              <td><%= exception_request.submitted_by_email %></td>
              <td><%= exception_request.lob_contact_name %></td>
              <td>
                <% if current_user.role == 'lob' &&  exception_request.try(:lxp_status) != 'APPROVED' %>
                  <%= link_to "Edit", edit_admin_exception_request_path(exception_request), class: "btn btn-xs btn-primary" %>
                <% end %> 
                <%= link_to "Review", admin_exception_request_path(exception_request), class: "btn btn-xs btn-secondary" %>	
              </td>
            </tr>
            <% end %>
          </tbody>
        </table>
      </div>
      <!-- end widget content -->
    </div>
    <!-- end widget div -->
  </div>
  <!-- end widget -->
</div>