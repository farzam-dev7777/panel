wb = xlsx_package.workbook

wb.styles do |style|
  project_heading = style.add_style(b: true, sz: 14)
  heading = style.add_style(b: true)

  wb.add_worksheet(name: "Exception Requests") do |sheet|
    # Add a title row
    #sheet.add_row ["Exception Requests List"], style: project_heading
    # Add the date this was downloaded
    #sheet.add_row ["Downloaded at", Time.now.strftime("%b %-d, %Y")]
    # Add a blank row
    #sheet.add_row []
    # Create the header row
    sheet.add_row ["Reason for request", "Matter Type"], style: heading
    # Create entries for each item
     @exception_requests.each do |exception_request|
      sheet.add_row [exception_request.reason.reject(&:blank?).join(", "), exception_request.matter_types_formated]
    end
  end
end