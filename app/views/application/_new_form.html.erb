<%= f.simple_fields_for :form_fields do |form_field| %>
  <div class="well">
    <%= render 'form_field_fields', f: form_field %>
  </div>
<% end %>
<div id="links">
  <p><strong><%= link_to_add_association 'Add Field', f, :form_fields, class: "btn btn-primary" %></strong></p>
</div>