<div class="col-lg-12">
  <div class="jarviswidget-color-darken" id="wid-id-0" data-widget-editbutton="false"> 
    <div class="page-header-sp m-top">
      <div class="page-title">Users</div>
      <div class="back-edit-actions">
        <div class="action-buttons-alignment">
          <a class="action-btn export-icon" href="<%= new_tenant_admin_user_path %>" data-toggle="tooltip" data-placement="top" title="Add new Tanant Admin">
            <span class="blue-text">Add new User</span> <%= image_tag "add.svg", alt:"Add new Tenant Admin" %>
          </a>
        </div>
      </div>
    </div>
    <div class="">
      <!-- widget content -->
      <div class="table-dyanmic-search table-responsive">
        <table id="dt_basic" class="table table-decoration table-bordered" width="100%">
          <thead>
            <tr>
              <th>Name</th>
              <th>Username</th>
              <th>Email</th>
              <th>Created At</th>
              <th>Action</th>
            </tr>
          </thead>
          <tbody>
            <% @users.each do |user| %>
              <tr onClick="window.location.href='/tenant_admin/users/<%= user.id %>/edit'">
                <td><%= [user&.first_name, user&.last_name]&.compact&.join(' ') %></td>
                <td><%= user&.username %></td>
                <td><%= user&.email %></td>
                <td><%= user&.created_at&.strftime('%d %b %Y at %I:%M %p') %></td>
                <td><%= link_to "Reset Password", reset_password_instructions_tenant_admin_user_path(user), method: :post, class: 'btn-type-default'%></td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
      <!-- end widget content -->
      
    </div>
  </div>
</div>
