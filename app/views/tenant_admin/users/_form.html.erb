<div class="sp-form sp-form-bg">
  <%= simple_form_for [:tenant_admin, @user] do |f| %>
    <div class="row form-flex-wrap">
      <div class="col-xs-12 col-md-6">
        <label class="label">First Name<span class="mandatory">*</span></label>
        <%= f.input :first_name, label: false, required: true %>
      </div>

      <div class="col-xs-12 col-md-6">
        <label class="label">Last Name</label>
        <%= f.input :last_name, label: false, required: true %>
      </div>

      <div class="col-xs-12 col-md-6">
        <label class="label">Password<span class="mandatory">*</span></label>
        <%= f.input :password, label: false, autocomplete: "off", input_html: { class: 'set-password' } %>
        <p style="color: #949494;" class="text-left"><em>Minimum 10 characters required</em></p>
      </div>

      <div class="col-xs-12 col-md-6">
        <label class="label">Confirm Password<span class="mandatory">*</span></label>
        <%= f.input :password_confirmation, label: false, autocomplete: "off", input_html: { class: 'set-password' } %>
        <p style="color: #949494;" class="text-left"><em>Minimum 10 characters required</em></p>
      </div>

      <% if @user.new_record? %>
        <div class="col-xs-12 col-md-6">
          <label class="label">Email<span class="mandatory">*</span></label>
          <%= f.input :email, label: false, required: true %>
        </div>

        <!-- <div class="col-xs-12 col-md-6">
          <label class="label">Username<span class="mandatory">*</span></label>
          <%#= f.input :username, label: false, required: true %>
        </div> -->
      <% else %>
        <div class="col-xs-12 col-md-6">
          <label class="label">Email<span class="mandatory">*</span></label>
          <%= f.input :email, label: false, autocomplete: "off", input_html: { disabled: true } %>
        </div>

        <div class="col-xs-12 col-md-6">
          <label class="label">Username<span class="mandatory">*</span></label>
          <%= f.input :username, label: false, autocomplete: "off"%>
        </div>
        
      <% end %>
      <div class="col-xs-12 col-md-6">
        <label class="label">Role<span class="mandatory">*</span></label>
        <%= f.input :role, label: false, as: :radio_buttons, collection: [['LOB', 'lob'], ['LXP', 'lxp'], ['Tenant Admin', 'tenant_admin'], ['Master User(Law Firm)', 'master_user'], ['Internal Lawyer', 'internal_lawyers'], ['User(Law Firm)', 'user']], input_html: { class: 'tenant_user_role' } %>
      </div>
      <% if ['master_user', 'user'].include?(@user.role)%>
        <div class="col-xs-12 col-md-6 tenant_law_firm">
      <% else %>
        <div class="col-xs-12 col-md-6 tenant_law_firm" style="display:none;">
      <% end %>
        <label class="label">LawFirm</label>
        <%= f.input :law_firm_id, label: false, collection: LawFirm.pluck(:name, :id), required: false%>
      </div>

      <% if ['lob', 'lxp'].include?(@user.role)%>
        <div class="col-xs-12 col-md-6 user_tenant">
      <% else %>
        <div class="col-xs-12 col-md-6 user_tenant" style="display:none;">
      <% end %>
        <label class="label">Tenant</label>
        <%= f.input :tenant_id, label: false, collection: Tenant.pluck(:name, :id), required: false%>
      </div>
      <div class="col-xs-12 col-md-12 mt-3">
        <%=link_to 'Cancel', tenant_admin_users_path, class: 'btn-type-default' %>
        <%= f.button :submit, @user.new_record? ? "Create" : "Update", class: "btn-type-primary" %>
        <%= link_to "Delete User", tenant_admin_user_path(@user), method: :delete, class: "btn-type-danger", data: { confirm: 'Are you certain you want to delete this?' } unless @user.new_record? %>
        <%= link_to "Reset Password", reset_password_instructions_tenant_admin_user_path(@user), method: :post, class: 'btn-type-default', data: { confirm: "Are you sure you would like to send password reset instructions to this user?" } unless @user.new_record? %>
        <%= link_to "Activate", activate_tenant_admin_user_path(@user), method: :post, class: 'btn-type-default', data: { confirm: "Are you sure you want to activate this user?" } if !@user.new_record? && @user.deactivated_at %>
        <%= link_to "De-Activate", deactivate_tenant_admin_user_path(@user), method: :post, class: 'btn-type-default', data: { confirm: "Are you sure you want to deactivate this user?" } if !@user.new_record? && !@user.deactivated_at%>
      </div>
      
    </div>
  <% end %>
</div>