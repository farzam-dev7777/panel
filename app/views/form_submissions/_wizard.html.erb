<div class="wizard form-wizard-steps">
  <ul class="steps">
    <%- steps.each_with_index do |step, index| %>
      <li data-target="#step1" class="text-center <%= (steps.find_index(step) <= steps.find_index(current_step)) ? 'active' : ''%>">
        <%
          
        %>
        <%= link_to wizard_path(step) do %>
          <%=
            case index
            when 0
              "<i class='fa fa-file'></i>".html_safe
            when 1
              "<i class='fa fa-user'></i>".html_safe
            when 2
              "<i class='fa fa-globe'></i>".html_safe
            when 3
              "<i class='fa fa-history'></i>".html_safe
            end
          %>
          <%= step.capitalize %> &nbsp;
            <% 
              stats = follow_up_stats
               if stats[step] %>
                (<%= stats[step] %>)
            <% end %>
          <span class="chevron"></span>
        <% end %>
      </li>
    <% end %>
  </ul>
</div>